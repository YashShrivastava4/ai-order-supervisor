# TODO(S5): Real Temporal worker entrypoint (python -m app.worker).
# Registers OrderSupervisorWorkflow + all activities.
# For now, use scripts/smoke_test_temporal.py to verify Temporal connectivity (S1).
