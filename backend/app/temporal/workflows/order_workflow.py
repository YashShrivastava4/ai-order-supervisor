# TODO(S5): OrderSupervisorWorkflow — state, signals, main loop.
# See 01_MASTER_SPEC.md §5. This is the real, long-lived workflow;
# do not confuse with backend/scripts/smoke_test_temporal.py (S1, throwaway).
