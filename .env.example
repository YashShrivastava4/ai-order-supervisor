# Optional for local dev — defaults to http://localhost:8000 if unset.
# Required when deploying the frontend (e.g. to Vercel): set this to the
# public URL of your running backend.
NEXT_PUBLIC_API_URL=http://localhost:8000
